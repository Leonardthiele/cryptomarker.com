<%= render "_form.html", Map.put(assigns, :action, exchange_exchange_currency_path(@conn, :update, @exchange, @exchange_currency)) %>

<%= if can?(@conn, :show, Exchange) do %>
  <span><%= link "Back", to: exchange_path(@conn, :show, @exchange) %></span>
<% end %>
